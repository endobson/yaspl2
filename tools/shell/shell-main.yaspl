#:module (tools shell shell-main)
#:import {
  (prim)
  (io)
}
(export)
(types)

(define (main [args : (Array Bytes)] [stdin : InputPort] [stdout : OutputPort] [stderr : OutputPort]) : Int
  (begin
    (write-line #"Hello World" stdout)
    0))
