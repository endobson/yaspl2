
In progress:

Things to do:

In libraries:
Make subbytes/bytes-copy! fast
Handle escape sequences
Add dictionary method that doesn't allow for overrides.

In compiler:
Handle bad input better than panic
Check bytes length when doing reads/writes

In interpreter:
Cond statements
Ensure variable scoping
Add : to type definitions
Provide better scoping of error messages.
Remove overheads from boxing
Make error messages for strings better
Allow annotation expression

In test suites:
Provide better scoping of error messages.
