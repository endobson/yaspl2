(module main
  (import
    (compiler [main cmain])
    (prim 
      #:types (Byte)
      #:values ()
      #:patterns ()))
  (export)
  (types)
  (define (main [args : (Array Bytes)] [stdin : InputPort] [stdout : OutputPort] [stderr : OutputPort]) : Byte
    (cmain args stdin stdout stderr)))
