(module main
  (import
    (compiler [main cmain])
    (prim 
      #:types (Byte)
      #:values ()
      #:patterns ()))
  (export)
  (types)
  (define (main [stdin : InputPort] [stdout : OutputPort] [stderr : OutputPort]) : Byte
    (cmain stdin stdout stderr)))
