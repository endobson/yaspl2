(module call3
  (types)
  (define (main [x : int64]) : int64
    (if (< (call fun 1) (call fun 2))
        (then
          (return (+ x 1)))
        (else
          (return (+ x 2)))))
  (define (fun [x : int64]) : int64
    (return x)))
