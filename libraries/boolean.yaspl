(module boolean
  (import
    (prim
      #:types (Boolean)
      #:values ()
      #:patterns ()))
  (export
    #:types ()
    #:values (not)
    #:patterns ())
  (types)

  (define (not [v : Boolean]) : Boolean
    (if v #f #t)))

