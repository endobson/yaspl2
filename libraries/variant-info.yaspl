#:module (variant-info)
#:import {
  (prim)
}
(export
  (#:types VariantInfo)
  (#:values variant-info)
  (#:patterns variant-info))

(types
  (define-type VariantInfo
    (variant-info
      [name : Bytes]
      [tag : U8])))
