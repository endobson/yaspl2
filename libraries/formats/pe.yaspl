#:module (formats pe)
#:import {
  (either)
  (prim)
}
(export
  (#:types PeFile)
  (#:values read-pe)
  (#:patterns))
(types
  (define-type PeFile
    (pe-file)))

(define (read-pe [input : InputPort]) : (Either Bytes PeFile)
  (left #"Not Yet Implemented"))
