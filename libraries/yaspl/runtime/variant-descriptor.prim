(module prim
  (types)
  (define (yprim_get_variant_name
            [input : (pointer (pointer undetermined-pointer))])
    : undetermined-pointer
    (def tagged-descriptor (ptr-ref input))
    ;; TODO do math here
    (def untagged-descriptor tagged-descriptor)
    (def variant-name (ptr-ref untagged-descriptor))
    (return variant-name)))
