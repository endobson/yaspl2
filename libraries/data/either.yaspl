(module either
  (import)
  (export
    #:types (Either)
    #:values (left right right-v)
    #:patterns (left right))
  (types
    (define-type (Either a b)
      (left [v a])
      (right [v b]))))
