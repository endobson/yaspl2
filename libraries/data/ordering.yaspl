#:module (data ordering)
#:import {
}
(export
  (#:types Ordering)
  (#:values less-than equal-to greater-than)
  (#:patterns less-than equal-to greater-than))
(types
  (define-type Ordering
    (less-than)
    (equal-to)
    (greater-than)))
