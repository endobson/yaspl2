#:module (types-test-utils)
#:import {
  (list) {
    #:values
      empty
      list
  }
  (types) {
    #:types
      Type
    #:values
      abstract-type
  }
}
(export
  #:types ()
  #:values (bytes-type int-type list-type)
  #:patterns ())
(types)

(define (bytes-type) : Type
  (abstract-type #"prim" #"Bytes" (empty)))

(define (int-type) : Type
  (abstract-type #"prim" #"Int" (empty)))

(define (list-type [t : Type]) : Type
  (abstract-type #"list" #"List" (varargs list t)))
