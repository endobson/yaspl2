#:module (env)
#:import {
  (io)
  (list)
  (prim)
}
(export)
(types)

(define (main [args : (Array Bytes)]
              [stdin : InputPort]
              [stdout : OutputPort]
              [stderr : OutputPort]
              [args2 : Bytes]) : Int
  (begin
    (write-line args2 stdout)
    0))
