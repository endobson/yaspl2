#:module (env)
#:import {
  (bytes)
  (io)
  (list)
  (prim)
}
(export)
(types)

(define (main [args : (Array Bytes)]
              [stdin : InputPort]
              [stdout : OutputPort]
              [stderr : OutputPort]
              [args2 : Bytes]) : Int
  (begin
    (for-each (lambda ([bytes : Bytes])
                 (write-line bytes stdout))
              (args->list args2))
    0))
